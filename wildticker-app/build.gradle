// Root build.gradle (minimal)
buildscript {
    repositories { google(); mavenCentral() }
    dependencies {
        classpath "com.google.dagger:hilt-android-gradle-plugin:2.47"
    }
}
allprojects { repositories { google(); mavenCentral() } }
